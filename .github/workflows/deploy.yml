name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Deploy to lyhadt.github.io
        run: |
          git clone --depth 1 --branch master https://x-access-token:${{ secrets.GH_PAT }}@github.com/lyh-ADT/lyh-adt.github.io.git /tmp/pages
          cd /tmp/pages
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          # 用 git rm 删除旧文件（兼顾历史清理）
          git rm -rf --ignore-unmatch docs/markdown-editor/ 2>/dev/null || true
          mkdir -p docs/markdown-editor
          cp -r $GITHUB_WORKSPACE/dist/* docs/markdown-editor/
          git add docs/markdown-editor/
          git commit -m "deploy: markdown-editor ${{ github.sha }}" || echo "nothing to deploy"
          git push origin master
